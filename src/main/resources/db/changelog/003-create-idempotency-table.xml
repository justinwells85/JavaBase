<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="003-create-idempotency-table" author="javabase">
        <comment>Create idempotency_keys table for idempotent request handling</comment>

        <createTable tableName="idempotency_keys">
            <column name="key" type="VARCHAR(255)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="response_status" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="response_body" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="expires_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <createIndex indexName="idx_idempotency_expires_at" tableName="idempotency_keys">
            <column name="expires_at"/>
        </createIndex>

        <rollback>
            <dropIndex indexName="idx_idempotency_expires_at" tableName="idempotency_keys"/>
            <dropTable tableName="idempotency_keys"/>
        </rollback>
    </changeSet>

    <changeSet id="003-create-processed-events-table" author="javabase">
        <comment>Create processed_events table for idempotent event handling</comment>

        <createTable tableName="processed_events">
            <column name="event_id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="processed_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <rollback>
            <dropTable tableName="processed_events"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
