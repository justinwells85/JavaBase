apiVersion: 1

providers:
  - name: 'JavaBase Dashboards'
    orgId: 1
    folder: 'JavaBase'
    folderUid: 'javabase'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
