apiVersion: v1
kind: Service
metadata:
  name: {{ include "javabase.fullname" . }}
  labels:
    {{- include "javabase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "javabase.selectorLabels" . | nindent 4 }}
